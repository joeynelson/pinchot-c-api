diff --git a/httplib.h b/httplib_internal.hpp
index 9ce4d86..142aa16 100644
--- a/httplib.h
+++ b/httplib_internal.hpp
@@ -291,6 +291,8 @@ struct Request {
 
   bool is_multipart_form_data() const;
 
+  bool is_json_req() const;
+
   bool has_file(const char *key) const;
   MultipartFormData get_file_value(const char *key) const;
 
@@ -2666,6 +2668,11 @@ inline bool Request::is_multipart_form_data() const {
   return !content_type.find("multipart/form-data");
 }
 
+inline bool Request::is_json_req() const {
+  const auto &content_type = get_header_value("Content-Type");
+  return !content_type.find("application/json");
+}
+
 inline bool Request::has_file(const char *key) const {
   return files.find(key) != files.end();
 }
@@ -3333,7 +3340,7 @@ inline bool Server::listen_internal() {
         break;
       }
 
-      auto val = detail::select_read(svr_sock_, 0, 100000);
+      auto val = detail::select_read(svr_sock_, 0, 1000000);
 
       if (val == 0) { // Timeout
         continue;
@@ -4783,4 +4790,4 @@ inline bool SSLClient::check_host_name(const char *pattern,
 
 } // namespace httplib
 
-#endif // CPPHTTPLIB_HTTPLIB_H
+#endif // CPPHTTPLIB_HTTPLIB_H
\ No newline at end of file
